<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="salon.salonpage.dao.SalonpageDao">
  	<select id="selectSalonpageList" resultType="salonpage">
  		select *
  			from salon
  		   order by no
  	</select>
  </mapper>