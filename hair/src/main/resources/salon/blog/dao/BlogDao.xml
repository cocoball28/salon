<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="salon.blog.dao.BlogDao">
	
	<!-- 본문  -->
	<!-- 본문  전체 리스트 -->
	<select id="selectBlogList" resultType="blog">
		select tag, content, image as imageFile, no 
		  from salon_blog
		 order by no
	</select>
	
	<!-- 본문 작성 후 한개 보여주기 -->
	<select id="registAndselectBlog" resultType="blog" parameterType="blog">
		select tag, content, image as imageFile, no
		  from salon_blog
		 where no = #{no}
	</select>
	
	<!-- 본문 작성  -->
	<insert id="insert" parameterType="blog">
		insert into salon_blog(tag, content, image) 
		values (#{tag}, #{content}, #{imageFile})
		<selectKey keyProperty="no" resultType="Integer" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
	
	<!-- 본문 삭제  -->
	<delete id="deleteBlog" parameterType="blog">
		delete FROM salon_blog
		where no = #{no}
	</delete>
	
	<!-- 파일 업로드  -->
	<insert id="insertImageFile" parameterType="blogImage">
		insert into salon_blog_image(save_full_file_name, file_name) 
		values (#{saveFullFileName}, #{fileName})
		<selectKey keyProperty="no" resultType="Integer" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
	
	
	
 
 
 
	<!-- 댓글 파트 -->
	<!-- 댓글 리스트 출력 -->
	 <select id="selectCommentList" parameterType="comment" resultType="comment">
	select comment_no as commentNo, no, content, reg_date as regDate, nick_name as nickName 
	  from salon_blog_comment
	 where no = #{no}
	 order by comment_no;
	 </select>
	 
	 <!-- 댓글 입력 후 댓글 출력 -->
	 <select id="selectComment" parameterType="comment" resultType="comment">
	 select comment_no as commentNo, no, content, reg_date as regDate, nick_name as nickName 
	 from salon_blog_comment
	 where no = #{no}
	 order by comment_no desc limit 1;
	 </select>
	 
	 <!-- 댓글 입력-->
	<insert id="insertComment" parameterType="comment">
	  insert into salon_blog_comment(no, content) 
	  values (#{no}, #{content})
	</insert>
	
	<!-- 댓글 삭제  -->
	<delete id="deleteComment" parameterType="comment">
		delete FROM salon_blog_comment
		where comment_no = #{commentNo}
	</delete>
</mapper>